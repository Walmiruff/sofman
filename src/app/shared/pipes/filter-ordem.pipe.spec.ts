import { FilterOrdemPipe } from './filter-ordem.pipe';

describe('FilterOrdemPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOrdemPipe();
    expect(pipe).toBeTruthy();
  });
});
